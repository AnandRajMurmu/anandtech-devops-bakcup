[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "anandtech-course-agents"
version = "0.1.0"
description = "Human-governed autonomous authoring agents for Journey with AnandTech"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
anandtech-agents = "anandtech_agents.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

